steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [ 
    'build', 
    '-t', '${_DOCKER_REPO}/flipbook-frontend', 
    '--build-arg', 'NEXT_PUBLIC_BACKEND_URL=${_NEXT_PUBLIC_BACKEND_URL}', 
    '.' 
  ]
images:
- '${_DOCKER_REPO}/flipbook-frontend'
